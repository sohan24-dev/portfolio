export const metadata = {
  title: "Services | SOHAN",
  description: "Web development services offered by SOHAN.",
};

export default function ServicesLayout({ children }) {
  return <>{children}</>;
}
