export const metadata = {
  title: "Projects | SOHAN",
  description: "Explore the latest projects and works by SOHAN.",
};

export default function ProjectsLayout({ children }) {
  return <>{children}</>;
}
