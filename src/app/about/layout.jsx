export const metadata = {
  title: "About | SOHAN",
  description: "Learn more about SOHAN, a Frontend Developer specialized in React, Next.js, and modern web technologies.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
