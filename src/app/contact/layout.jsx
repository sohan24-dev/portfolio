export const metadata = {
  title: "Contact | SOHAN",
  description: "Get in touch with SOHAN for collaborations and projects.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
